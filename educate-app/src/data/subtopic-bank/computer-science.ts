import type { SubtopicContent } from '@/types';

export const computerScienceSubtopics: Record<string, Record<string, SubtopicContent>> = {
  "Algorithms": {
    "Searching Algorithms": {
      short: [
        { question: "Describe how a linear search works.", answer: "A linear search checks each item in a list one by one, from the first to the last, until the target is found or the end of the list is reached. It works on unsorted lists. Best case: O(1) (first element); worst case: O(n) (last element or not found). It is simple but inefficient for large datasets.", marks: 2, hint: "Check each item one by one from start; works on unsorted lists; O(n) worst case" },
        { question: "What is the key requirement for a binary search, and why does it make binary search more efficient than linear search for large lists?", answer: "Binary search requires the list to be sorted. It works by repeatedly halving the search space: compare the target to the middle element — if smaller, search the left half; if larger, search the right half. This gives O(log n) time complexity. For 1,000,000 elements, linear search may take 1,000,000 comparisons; binary search takes at most 20 (since log₂(1,000,000) ≈ 20).", marks: 2, hint: "Must be sorted; halves search space each step; O(log n) vs O(n) — give numerical example" },
      ],
      mid: [
        { question: "Trace through a binary search for the value 42 in the sorted list [5, 12, 23, 37, 42, 56, 78, 91].", answer: "List: [5, 12, 23, 37, 42, 56, 78, 91] (indices 0-7). Step 1: low=0, high=7, mid=3. List[3]=37. 42>37, so low=4. Step 2: low=4, high=7, mid=5. List[5]=56. 42<56, so high=4. Step 3: low=4, high=4, mid=4. List[4]=42. Found at index 4. Total: 3 comparisons (vs up to 5 for linear search).", marks: 4, hint: "Show low, high, mid at each step; compare middle value to target; adjust low or high; show which half is discarded each time" },
      ],
      long: [
        { question: "Compare linear search and binary search. When would you choose each algorithm, and what are their relative advantages and limitations?", answer: "Linear search and binary search are both fundamental searching algorithms, but they differ significantly in their requirements, efficiency and appropriate use cases. Linear search operates by sequentially checking each element from start to finish until the target is found or the list is exhausted. Its time complexity is O(n) in the worst case — directly proportional to list size. It requires no pre-processing: the list does not need to be sorted. This makes linear search the only option for unsorted data, and it is entirely appropriate for small datasets where simplicity is valued. For a list of 10 items, the difference between linear and binary search is negligible. Binary search's efficiency advantage only becomes significant at scale: for 1,024 elements, binary search takes at most 10 comparisons (log₂1024 = 10), while linear search may take 1,024. However, binary search requires the list to be sorted — a prerequisite that carries its own cost. Sorting algorithms like quicksort (O(n log n) average) or merge sort (O(n log n)) must be applied before binary search is possible. If a list is searched only once and is unsorted, the combined cost of sorting + binary search may exceed the cost of a single linear search. Binary search is most advantageous when: the list is already sorted (or is sorted once and searched many times); the dataset is large; and the O(log n) efficiency gain is meaningful. In database index structures (B-trees), binary search principles underpin how records are located among millions of entries with minimal comparisons. The choice between algorithms illustrates a broader principle in computer science: no algorithm is universally superior — efficiency depends on context, data characteristics and use frequency.", marks: 8, hint: "Linear: O(n), works on unsorted, simple, good for small lists. Binary: O(log n), must be sorted, highly efficient for large lists. Compare with numbers (1024 elements: 10 vs 1024 comparisons). When to sort first. B-trees as real application. Conclusion: no universally better algorithm — context determines choice." },
      ],
      flashcard: [
        { term: "Binary Search — Time Complexity", definition: "Binary search has O(log n) time complexity — with each comparison, the search space is halved. This means for n=1,048,576 (2²⁰) items, binary search requires at most 20 comparisons. By contrast, linear search has O(n) — potentially 1,048,576 comparisons. Binary search requires the data to be sorted first.", example: "For a phone directory of 10 million entries, binary search finds a name in at most 24 steps; linear search could take 10 million steps." },
        { term: "Linear Search — Use Cases", definition: "Linear search checks each element sequentially from start to end. Time complexity: O(n). Works on unsorted lists — the only searching option for unordered data. Appropriate for: small datasets; unsorted data; one-time searches where sorting cost would exceed search savings; linked lists where random access is not possible.", example: "Searching a small, frequently changing list (e.g. recent browser history) — sorting overhead not worthwhile for a linear search that completes instantly." },
      ],
    },
    "Sorting Algorithms": {
      short: [
        { question: "Describe how bubble sort works.", answer: "Bubble sort repeatedly compares adjacent elements in a list and swaps them if they are in the wrong order. After each pass through the list, the largest unsorted element 'bubbles' to its correct position at the end. The algorithm repeats until no swaps are needed (list is sorted). Time complexity: O(n²) average and worst case; O(n) best case (already sorted with optimisation).", marks: 2, hint: "Compare adjacent pairs, swap if wrong order; largest element bubbles to end each pass; O(n²)" },
        { question: "What is the difference between merge sort and bubble sort in terms of efficiency?", answer: "Merge sort uses a divide-and-conquer approach: repeatedly splits the list in half, sorts each half recursively, then merges sorted halves. Time complexity: O(n log n) in all cases. Bubble sort has O(n²) average/worst case. For n=1000, bubble sort may perform ~500,000 comparisons; merge sort ~10,000. Merge sort is significantly more efficient for large datasets but uses more memory (not in-place).", marks: 2, hint: "Merge sort: divide-and-conquer, O(n log n); bubble sort: O(n²); numerical comparison; merge sort uses more memory" },
      ],
      mid: [
        { question: "Trace through one complete pass of bubble sort on the list [5, 2, 8, 1, 9].", answer: "Pass 1: Compare 5,2 → swap → [2,5,8,1,9]. Compare 5,8 → no swap → [2,5,8,1,9]. Compare 8,1 → swap → [2,5,1,8,9]. Compare 8,9 → no swap → [2,5,1,8,9]. After pass 1, the largest (9) is in its correct position at the end. The list is not yet sorted — further passes are needed. After pass 2: [2,1,5,8,9]. After pass 3: [1,2,5,8,9]. Sorted.", marks: 4, hint: "Compare each adjacent pair in order; show each comparison with swap/no swap; indicate that largest element reaches its final position after pass 1" },
      ],
      long: [
        { question: "Evaluate the use of different sorting algorithms in computer science. Consider efficiency, use cases and limitations.", answer: "Sorting algorithms are fundamental to computer science — sorted data enables efficient searching, database queries, statistical analysis and many other operations. The choice of sorting algorithm involves trade-offs between time complexity, space complexity, stability and practical behaviour. Bubble sort is the simplest: repeatedly comparing and swapping adjacent elements until sorted. Its O(n²) time complexity makes it impractical for large datasets — sorting 1 million elements might take 10¹² operations. Its only practical advantage is simplicity (useful for teaching) and O(n) performance on nearly-sorted data with optimisation. Insertion sort also has O(n²) worst case but performs well on small or nearly-sorted lists, and is the algorithm of choice in practice for datasets under ~50 elements (low overhead). It is adaptive — takes advantage of existing order. Merge sort uses divide-and-conquer: split, sort recursively, merge. O(n log n) in all cases makes it reliable and predictable. It is stable (preserves relative order of equal elements) — important for database sorting. Disadvantage: requires O(n) additional space (not in-place). Quicksort is O(n log n) average but O(n²) worst case (when always picking the smallest or largest as pivot). In practice, with good pivot selection (random or median-of-three), quicksort is often faster than merge sort due to better cache performance and being in-place. It is the typical implementation choice for language standard libraries (e.g. Python's timsort — a hybrid of merge sort and insertion sort — uses insertion sort for small sub-arrays and merge sort for larger ones). The key principle is that algorithm choice depends on context: dataset size, whether data is nearly sorted, memory constraints and whether stability is required. No single algorithm is optimal for all situations — practical implementations often use hybrids.", marks: 8, hint: "Bubble sort: O(n²), simple, impractical at scale. Insertion sort: O(n²) but good for small/nearly-sorted. Merge sort: O(n log n) guaranteed, stable, but O(n) space. Quicksort: O(n log n) average, in-place, faster in practice but O(n²) worst case. Timsort as hybrid. Conclusion: no single best — context determines choice." },
      ],
      flashcard: [
        { term: "Merge Sort — Divide and Conquer", definition: "Merge sort recursively splits a list in half until individual elements remain (base case), then merges sorted pairs back together in order. Time complexity: O(n log n) in all cases (best, average, worst). Space complexity: O(n) — requires additional memory for merging. Stable sort — preserves relative order of equal elements.", example: "[5,2,8,1] → split → [5,2] and [8,1] → split → [5],[2],[8],[1] → merge → [2,5],[1,8] → merge → [1,2,5,8]" },
        { term: "Time Complexity Notation (Big-O)", definition: "Big-O notation describes algorithm efficiency — how runtime grows with input size n. O(1) = constant; O(log n) = logarithmic (binary search); O(n) = linear (linear search); O(n log n) = linearithmic (merge sort); O(n²) = quadratic (bubble sort). Lower complexity = more efficient for large inputs.", example: "For n=1,000,000: O(log n)≈20 operations; O(n)=1,000,000; O(n²)=1,000,000,000,000 — quadratic algorithms are impractical at scale." },
      ],
    },
  },
  "Programming": {
    "Selection (if/else)": {
      short: [
        { question: "Write pseudocode that checks if a number is positive, negative or zero.", answer: "INPUT num\nIF num > 0 THEN\n  OUTPUT 'Positive'\nELSEIF num < 0 THEN\n  OUTPUT 'Negative'\nELSE\n  OUTPUT 'Zero'\nENDIF", marks: 2, hint: "Three conditions: > 0, < 0, and the ELSE case (zero); use IF/ELSEIF/ELSE structure" },
        { question: "What is the difference between an IF statement and a CASE (switch) statement?", answer: "An IF/ELSEIF statement evaluates a boolean condition and can test different variables or complex expressions at each branch. A CASE (switch) statement tests a single variable against multiple specific values — it is more readable and efficient when comparing one variable against many possible values. CASE statements are typically compiled to lookup tables, potentially faster than multiple IF comparisons.", marks: 2, hint: "IF: evaluates boolean conditions (can be complex, different variables); CASE: compares one variable against specific values, more readable for multiple options" },
      ],
      mid: [
        { question: "Write a program (pseudocode or Python) that asks the user for their age and outputs an appropriate category: Child (<13), Teenager (13-17), Adult (18-64), Senior (65+).", answer: "Python:\nage = int(input('Enter your age: '))\nif age < 13:\n    print('Child')\nelif age <= 17:\n    print('Teenager')\nelif age <= 64:\n    print('Adult')\nelse:\n    print('Senior')\n\nNote: elif (else if) checks conditions in order — once one is True, subsequent conditions are not checked. The order matters: checking < 13 first means 'Adult' branch only reached if age >= 18.", marks: 4, hint: "Use if/elif/else (not multiple separate if statements); show that conditions are checked in order; boundary values must be correct (13, 17, 64, 65)" },
      ],
      long: [
        { question: "Explain the importance of selection statements in programming. Give examples of how they control program flow and how poorly written selection can cause bugs.", answer: "Selection statements — IF, ELSEIF, ELSE and CASE/SWITCH — are one of the three fundamental control structures in programming (alongside sequence and iteration). They allow programs to make decisions and execute different code paths based on conditions, making programs dynamic and responsive to input rather than following a single fixed sequence. Without selection, a program could not respond differently to different inputs — it could not validate data, handle errors or implement any conditional logic. The boundary between branches (the condition itself) is a critical point of correctness. Off-by-one errors in conditions are a common source of bugs: using `> 17` instead of `>= 18` for 'Adult' would exclude 18-year-olds from the adult category. Similarly, conditions that overlap (e.g. checking age < 20 AND age < 18 without proper ordering) can cause logical errors. Missing ELSE branches are another frequent problem: if all IF/ELSEIF branches fail and no ELSE exists, the program may proceed without any action, potentially leaving variables uninitialised. Short-circuit evaluation in languages like Python and Java means that in `if a > 0 and b/a > 2`, if a=0 the division is never evaluated (avoiding division by zero) — understanding this behaviour is important for defensive programming. Nested IF statements (IF within IF) can handle complex multi-dimensional conditions but reduce readability and increase the risk of incorrect bracket/indentation structure. Guard clauses (early return/exit when conditions are not met) are preferred in modern programming to deep nesting. CASE/SWITCH statements improve readability when one variable is compared against many specific values — compilers often optimise these into jump tables. The key principle is that every selection branch must be logically complete: all valid inputs must be handled correctly, and edge cases (zero, negative numbers, empty strings, very large values) must be explicitly considered and tested.", marks: 8, hint: "Why selection is essential (program responds to input); boundary conditions (off-by-one errors); missing ELSE problems; short-circuit evaluation; nested IF problems (readability, bracket errors); guard clauses; CASE vs IF/ELIF; testing edge cases. Give code examples throughout." },
      ],
      flashcard: [
        { term: "Nested IF Statements", definition: "A nested IF is an IF statement inside another IF statement, allowing complex multi-condition logic. While necessary in some cases, deep nesting reduces code readability and increases the risk of logical errors. Modern programming practice favours 'guard clauses' (early exit when conditions not met) and logical operators (AND/OR) to reduce nesting depth.", example: "if grade >= 70:\n    if grade >= 90:\n        print('A')\n    else:\n        print('B')\n— better written with elif: if grade >= 90: ... elif grade >= 70: ..." },
        { term: "Boolean Conditions in Selection", definition: "Selection statements evaluate Boolean (True/False) expressions. Common comparison operators: == (equal), != (not equal), > (greater than), < (less than), >= (greater than or equal), <= (less than or equal). Logical operators: AND (both true), OR (at least one true), NOT (inverts). Understanding operator precedence and short-circuit evaluation prevents logic errors.", example: "if age >= 18 and has_id == True: — both conditions must be true; if age >= 65 or has_senior_card: — either condition sufficient." },
      ],
    },
  },
  "Computer Systems": {
    "CPU Architecture & FDE Cycle": {
      short: [
        { question: "What are the three main components of a CPU and what does each do?", answer: "1) Control Unit (CU) — controls and coordinates all CPU operations; fetches, decodes and executes instructions; sends control signals to other components. 2) Arithmetic Logic Unit (ALU) — performs arithmetic calculations (add, subtract) and logical operations (AND, OR, compare). 3) Registers — tiny, extremely fast temporary storage locations within the CPU (e.g. Program Counter, Accumulator, Memory Address Register, Memory Data Register).", marks: 3, hint: "CU (coordinates, fetch-decode-execute), ALU (arithmetic and logical operations), Registers (tiny fast storage: PC, ACC, MAR, MDR)" },
        { question: "Describe the Fetch-Decode-Execute (FDE) cycle.", answer: "Fetch: the Control Unit fetches the next instruction from RAM using the address in the Program Counter (PC); the instruction is loaded into the Current Instruction Register (CIR); the PC is incremented to point to the next instruction. Decode: the CU decodes the instruction to determine what operation is needed. Execute: the ALU or other components carry out the instruction (arithmetic, data movement, branching). The cycle repeats continuously.", marks: 3, hint: "Fetch: PC → MAR → instruction into CIR → PC incremented. Decode: CU interprets instruction. Execute: ALU or other component carries out operation. Repeats." },
      ],
      mid: [
        { question: "Explain how clock speed, number of cores and cache size affect CPU performance.", answer: "Clock speed (measured in GHz) is the number of instruction cycles per second. A higher clock speed means more FDE cycles per second — faster execution of a single-threaded program. However, increasing clock speed generates more heat and has diminishing returns. Cores: a multi-core processor has multiple independent processing units that can execute instructions simultaneously. Dual-core can theoretically double single-core performance for multi-threaded programs, but depends on software being written to use multiple cores (parallelism). Cache: faster than RAM but smaller; stores frequently accessed data/instructions close to the CPU. L1 cache (smallest, fastest) is checked first; then L2, L3. A larger cache reduces the frequency of slower RAM access (cache misses), improving performance. All three factors interact: a 4-core processor with high clock speed but small cache may underperform a 2-core with large cache for cache-sensitive tasks.", marks: 4, hint: "Clock speed: FDE cycles per second, more heat, diminishing returns. Cores: parallel execution, depends on multi-threaded software. Cache: faster than RAM, reduces cache misses, L1/L2/L3 hierarchy. They interact — no single factor determines performance alone." },
      ],
      long: [
        { question: "Explain how data is stored and processed in a computer system, covering binary representation, the role of RAM and ROM, and how the CPU accesses data.", answer: "All data in a computer system is stored and processed in binary — sequences of 0s and 1s — because electronic circuits can reliably represent two states (on/off, high/low voltage). A single binary digit is a bit; 8 bits form a byte, the fundamental unit of data storage. Text is represented using character encoding standards like ASCII (7-bit, 128 characters) or Unicode (UTF-8, covering all world languages). Images use binary values for pixel colour (in RGB, each channel 0-255 = 8 bits; a 24-bit image uses 3 bytes per pixel). Sound is digitised by sampling the analogue waveform at regular intervals (sample rate in Hz) and recording each sample's amplitude as a binary number (bit depth). The computer's memory hierarchy ranges from fast-to-slow and small-to-large. Registers (within the CPU) are the fastest and smallest — single words (32/64 bits); they hold data being immediately processed. Cache memory (L1/L2/L3) is SRAM-based, very fast (nanoseconds) and holds recently accessed instructions and data to reduce RAM access frequency. RAM (Random Access Memory) is the main working memory — DRAM-based, volatile (loses data when powered off), holding currently running programs and their data. ROM (Read-Only Memory) is non-volatile and stores firmware/BIOS — instructions needed to start the computer before the operating system loads. Secondary storage (SSD, HDD) is non-volatile long-term storage — much slower than RAM but retaining data permanently. The CPU accesses data through the FDE cycle using address, data and control buses. When the CPU needs data, it places the memory address in the Memory Address Register (MAR); the data is retrieved and placed in the Memory Data Register (MDR); the ALU processes it; results are stored back. The bus width (number of parallel wires) determines how much data transfers simultaneously. The performance bottleneck between fast CPUs and slower RAM is mitigated by cache — storing frequently accessed data closer to the processor.", marks: 8, hint: "Binary: bits/bytes, text (ASCII/Unicode), images (RGB pixels), sound (sampling rate, bit depth). Memory hierarchy: registers (fastest/smallest), cache (SRAM, L1/L2/L3), RAM (DRAM, volatile), ROM (non-volatile, BIOS), secondary storage (HDD/SSD, non-volatile). FDE cycle data access: MAR, MDR, address/data/control buses, bus width. Cache reducing RAM bottleneck." },
      ],
      flashcard: [
        { term: "Program Counter (PC)", definition: "A special register in the CPU that holds the memory address of the next instruction to be fetched. During the Fetch stage of the FDE cycle, the PC's value is copied to the Memory Address Register (MAR), the instruction is retrieved, and the PC is automatically incremented to point to the following instruction (or updated by a jump/branch instruction).", example: "If PC=1000, the CPU fetches the instruction at memory address 1000, then PC becomes 1001 — unless the instruction is a jump to address 2000, in which case PC=2000." },
        { term: "Von Neumann Architecture", definition: "The design principle underlying most modern computers: a single memory stores both program instructions and data (not separate); a single bus connects memory and CPU; instructions are fetched and executed sequentially by the FDE cycle. Named after mathematician John von Neumann (1945). The 'Von Neumann bottleneck' — the single bus limiting data transfer speed — is a fundamental constraint mitigated by cache.", example: "Your PC, phone and games console all use Von Neumann architecture — a CPU fetching instructions from RAM, processing data and storing results back in the same memory." },
      ],
    },
  },
  "Networks": {
    "Network Topologies": {
      short: [
        { question: "Describe the difference between a star network topology and a bus network topology.", answer: "Star topology: all devices connect to a central switch or hub. If one device fails, others are unaffected. If the central switch fails, the whole network fails. High performance (dedicated connections). Expensive (more cable). Bus topology: all devices connect to a single cable (the bus/backbone). Easy to set up, cheap. If the bus fails, the whole network fails. Performance degrades with more devices (only one device can transmit at a time — collisions). Bus is largely obsolete in modern networks.", marks: 2, hint: "Star: central switch, one failure doesn't affect others but switch failure = network down. Bus: single cable, cheap, whole network fails if cable fails, collisions" },
        { question: "What is the difference between a LAN and a WAN?", answer: "LAN (Local Area Network): covers a small geographical area (single building, school, office). Hardware (cables, switches) is owned by the organisation. High speed, low cost per connection. WAN (Wide Area Network): covers a large geographical area (cities, countries, worldwide). Infrastructure (telephone lines, fibre optic cables, satellites) is typically leased from telecommunications providers. Lower speed than LAN, higher cost. The Internet is the world's largest WAN.", marks: 2, hint: "LAN: small area, owned hardware, high speed. WAN: large area, leased infrastructure, lower speed. Internet = largest WAN." },
      ],
      mid: [
        { question: "Explain what protocols are and why they are needed in computer networks. Give two examples.", answer: "Protocols are agreed sets of rules that govern how data is transmitted between devices on a network. Without protocols, devices from different manufacturers using different software could not communicate — protocols provide a common language. Examples: 1) TCP/IP (Transmission Control Protocol / Internet Protocol) — governs how data is split into packets, addressed, routed across the Internet and reassembled at the destination. TCP ensures packets arrive and requests retransmission if lost. 2) HTTP/HTTPS (HyperText Transfer Protocol [Secure]) — governs how web browsers and web servers communicate to request and deliver web pages. HTTPS adds encryption (SSL/TLS) for security. Other examples: FTP (file transfer), SMTP (email sending), DNS (domain name to IP address translation).", marks: 4, hint: "Definition: agreed rules for communication; why needed: interoperability between different devices/manufacturers. Examples: TCP/IP (packet routing, reassembly), HTTP/HTTPS (web, with HTTPS adding encryption). Could also mention FTP, SMTP, DNS." },
      ],
      long: [
        { question: "Explain the threats to network security and evaluate the methods used to protect against them.", answer: "Networks face a range of threats that can compromise data confidentiality, integrity and availability — the CIA triad of information security. Understanding these threats and appropriate countermeasures is essential for any networked organisation. Malware (malicious software) encompasses viruses (self-replicating code that attaches to legitimate files), worms (self-propagating across networks without user action), Trojans (disguised as legitimate software) and ransomware (encrypts data and demands payment for decryption). The NHS WannaCry attack (2017) paralysed hospitals across 80 countries using ransomware spread through unpatched Windows vulnerabilities. Protection: anti-malware software, regular operating system patches, user education (not opening suspicious attachments), application whitelisting. Phishing attacks use deceptive emails or websites impersonating legitimate organisations to steal credentials or install malware. Spear phishing targets specific individuals using personal information. Protection: email filtering, user training, multi-factor authentication (even if credentials are stolen, a second factor — authenticator app, SMS code — prevents access), SPF/DMARC email authentication. Denial of Service (DoS) and Distributed DoS (DDoS) attacks flood a server with traffic to make it unavailable. Protection: rate limiting, traffic analysis, CDN (Content Delivery Networks that distribute load), ISP-level filtering. SQL injection attacks insert malicious SQL code into web form inputs to manipulate databases — potentially extracting all data or deleting records. Protection: input validation/sanitisation, parameterised queries, principle of least privilege (database accounts limited to necessary permissions). Man-in-the-Middle (MitM) attacks intercept communications between two parties. Protection: encryption (HTTPS/TLS), VPNs, certificate verification. Firewall is the foundational network protection: filtering incoming/outgoing traffic against rules, blocking unauthorised access. However, firewalls alone are insufficient — modern threats bypass them through social engineering and legitimate-looking traffic. Defence-in-depth — multiple overlapping security layers — is the accepted principle: no single measure is sufficient; breach of one layer must not compromise the whole system. Regular penetration testing (ethical hacking) identifies vulnerabilities before attackers. The human element (social engineering) remains the most common attack vector — technical measures must be supported by staff training and security culture.", marks: 8, hint: "Threats: malware (virus/worm/Trojan/ransomware — NHS WannaCry), phishing/spear phishing, DDoS, SQL injection, MitM. Countermeasures: anti-malware, patches, MFA, email filtering, input validation/parameterised queries, HTTPS/TLS, firewalls, VPNs, pen testing. Principle of defence-in-depth. Human element/social engineering. CIA triad concept." },
      ],
      flashcard: [
        { term: "TCP/IP Protocol Suite", definition: "TCP (Transmission Control Protocol) and IP (Internet Protocol) form the foundation of Internet communication. IP handles addressing and routing — each device has an IP address; packets are routed across the network using these addresses. TCP ensures reliable delivery — it breaks data into packets, ensures all arrive (requesting retransmission of lost packets) and reassembles them in correct order.", example: "When you load a webpage, HTTP requests travel over TCP/IP: the request is split into packets, each routed independently across the Internet, reassembled at the web server, which sends the page data back the same way." },
        { term: "Packet Switching", definition: "Data transmission method where data is split into small packets, each independently routed across the network and reassembled at the destination. Different packets in the same message may take different routes. Advantages: efficient use of bandwidth (multiple messages share network); robust (if one route fails, packets reroute); no dedicated connection needed. Used by the Internet.", example: "A 10MB file split into thousands of packets — each packet takes the fastest available route at the time; all arrive and are reassembled into the complete file." },
      ],
    },
  },
};
